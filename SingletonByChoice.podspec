d::Spec.new do |s|
  s.name     = 'SingletonByChoice'
  s.version  = '1.0'
  s.license  = 'MIT'
  s.summary  = 'Parent class for creating Objective-C Singletons. XCTest unit tests included.'
  s.homepage = 'https://github.com/rlisle/SingletonByChoice'
  s.author   = { 'Ron Lisle' => 'singletonbychoice@gmail.com' }
  s.source   = { :git => 'https://github.com/rlisle/SingletonByChoice'}
  s.source_files = 'SingletonByChoice.{h,m}'

end

